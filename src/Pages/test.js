import React from "react";
import { useSelector } from "react-redux";

const TestComponent = () => {
  // const userData = useSelector((state) => state.kanban);
  // console.log("🚀 ~ file: test.js:6 ~ TestComponent ~ userData:", userData)
  return (
    <>
      <h4>Test</h4>
    </>
  );
};

export default TestComponent;
